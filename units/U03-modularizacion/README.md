# Unidad 3 — Modularización (Procedimientos y Macros)

Temas 3.1–3.2 del temario: subrutinas, paso de parámetros según el ABI de AArch64,
preservación de registros, stack frames y macros del ensamblador GNU (`.macro`/`.endm`).

## Contenido
- `lecturas/lecture04.md` — Subrutinas y paso de parámetros en ARM64 (rescatado del acervo 2024).

## Por desarrollar (semestre 2026)
- Lección de macros GNU as: `.macro`, `.include`, parámetros y macros anidadas.
- Práctica: biblioteca de rutinas reutilizables (imprimir cadena, imprimir número decimal)
  invocada desde C (`gcc` como driver) — interop C ↔ ASM.
- Comparativa procedimiento vs macro: tamaño de binario y rendimiento.
